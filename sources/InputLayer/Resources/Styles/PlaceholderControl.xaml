<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:InputLayer.Controls">

    <Style x:Key="PlaceholderControlStyle" TargetType="{x:Type controls:PlaceholderControl}">
        <Setter Property="Padding" Value="6,0,0,0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:PlaceholderControl}">
                    <Grid>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" />

                        <TextBlock x:Name="PART_PlaceholderText"
                                   Margin="{TemplateBinding Padding}"
                                   VerticalAlignment="Center"
                                   FontStyle="Italic"
                                   Foreground="{Binding RelativeSource={RelativeSource TemplatedParent},
                                                        Path=Content.(Control.Foreground)}"
                                   IsHitTestVisible="False"
                                   Opacity="0.6"
                                   Text="{TemplateBinding Placeholder}"
                                   Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEmpty" Value="True" />
                                <Condition Property="IsContentFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_PlaceholderText" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource PlaceholderControlStyle}" TargetType="{x:Type controls:PlaceholderControl}" />
</ResourceDictionary>