<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:actions="clr-namespace:InputLayer.Common.Models.Actions;assembly=InputLayer.Common"
                    xmlns:controls="clr-namespace:InputLayer.Controls"
                    xmlns:converters="clr-namespace:InputLayer.Converters"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:me="clr-namespace:InputLayer.MarkupExtensions"
                    xmlns:settings="clr-namespace:InputLayer.Common.Models.Actions.Settings;assembly=InputLayer.Common"
                    xmlns:templateSelectors="clr-namespace:InputLayer.TemplateSelectors"
                    mc:Ignorable="d">

    <DataTemplate x:Key="GameControllerActionRumbleSettings" DataType="{x:Type actions:GameControllerAction}">
        <controls:GridWithGap d:DataContext="{d:DesignInstance settings:GameControllerActionRumbleSettings}"
                              ColumnGap="10"
                              DataContext="{Binding Settings}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <HeaderedContentControl Grid.Column="0" Foreground="{DynamicResource TextBrush}">
                <HeaderedContentControl.Header>
                    <MultiBinding Converter="{converters:StringFormatConverter}">
                        <me:LocalizationBinding Key="InputLayer.GameControllerAction.Rumble.Settings.Duration" />
                        <Binding Path="DurationMs" />
                    </MultiBinding>
                </HeaderedContentControl.Header>

                <Slider IsSnapToTickEnabled="True"
                        Maximum="1000"
                        Minimum="100"
                        TickFrequency="100"
                        TickPlacement="BottomRight"
                        Value="{Binding DurationMs}" />
            </HeaderedContentControl>

            <HeaderedContentControl Grid.Column="1" Foreground="{DynamicResource TextBrush}">
                <HeaderedContentControl.Header>
                    <MultiBinding Converter="{converters:StringFormatConverter}">
                        <me:LocalizationBinding Key="InputLayer.GameControllerAction.Rumble.Settings.Intensity" />
                        <Binding Path="Intensity" />
                    </MultiBinding>
                </HeaderedContentControl.Header>
                <Slider IsSnapToTickEnabled="True"
                        Maximum="1.0"
                        Minimum="0.1"
                        TickFrequency="0.1"
                        TickPlacement="BottomRight"
                        Value="{Binding Intensity}" />
            </HeaderedContentControl>

        </controls:GridWithGap>
    </DataTemplate>

    <templateSelectors:GameControllerActionSettingsSelector x:Key="GameControllerActionSettingsSelector" RumbleTemplate="{StaticResource GameControllerActionRumbleSettings}" />

</ResourceDictionary>